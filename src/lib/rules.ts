/**
 * The two sentences the app repeats about sets.
 *
 * They live here rather than in the screen that shows them so that the
 * workout, the guide and the help sheets cannot drift into saying three
 * slightly different things about the same rule.
 */

/**
 * The warm-up rule, stated once: the first exercise of a day gets a four set
 * pyramid at 50, 70, 85 and 90 percent of the weight you are about to lift,
 * and only when it is a barbell lift. Nothing else in the day gets rows.
 */
export const WARMUP_RULE =
  'Built for you from the weight you are about to lift, and left out of your ' +
  'records and your volume. Only the first exercise of a day gets them, and ' +
  'only when it loads a barbell'

/** What a working set is, and what the range is asking of it. */
export const WORKING_RULE =
  'The sets that count. The rep range is the target for each of them, and ' +
  'clearing the top of it on every set is what moves the weight up next time'
